import 'package:flutter/material.dart';

class AppColors {
  // Primary colors - GitHub-like blue
  static const Color primary = Color(0xFF2188FF);
  static const Color primaryVariant = Color(0xFF1877F2);

  // Secondary colors - VS Code-like
  static const Color secondary = Color(0xFF4EC9B0);
  static const Color secondaryVariant = Color(0xFF3FA697);

  // Background colors - Dark theme inspired
  static const Color background = Color(0xFF1E1E1E);
  static const Color surface = Color(0xFF252526);
  static const Color surfaceLight = Color(0xFF2D2D2D);

  // Text colors
  static const Color onPrimary = Colors.white;
  static const Color onSecondary = Colors.black;
  static const Color onBackground = Colors.white;
  static const Color onSurface = Colors.white;
  static const Color textSecondary = Color(0xFF8A8A8A);

  // Accent colors
  static const Color success = Color(0xFF4EC9B0); // VS Code green
  static const Color warning = Color(0xFFCE9178); // VS Code orange
  static const Color error = Color(0xFFE51400); // VS Code red
  static const Color info = Color(0xFF9CDCFE); // VS Code blue

  // Status colors
  static const Color onSuccess = Colors.white;
  static const Color onWarning = Colors.black;
  static const Color onError = Colors.white;
  static const Color onInfo = Colors.black;
}
